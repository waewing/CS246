#include <iostream>
#include "linkedlist.h"
using namespace std;

main()
{
   bool Palindrome(const LinkedList<char>& word)
   {
       LinkedList<char> cword = word;
       if(word.Length(cword < 3))
       {
           throw 'Out of Bound';
       }
       else
       {
           for(int i = 0; i < cword.Length(); i++)
           {
               if(cword.GetData() = cword.GetData()->
           }
       }
       
   }

}